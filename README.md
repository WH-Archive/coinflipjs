# CoinflipJS
Flip a coin

## Usage
```js
// Import Package
const coinflip = require("coinflipjs");

// Generate result
// Returns either "heads" or "tails"
const res = coinflip();

// Log the result
console.log(res);
```
